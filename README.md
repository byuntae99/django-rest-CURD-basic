# django-rest-CURD-basic

search for django restframework

In Terminal pip install restframework

INSTALLED_APPS = [ ....
'rest_framework',
] -------------add restframework in the project settings INSTALLED_APPS
